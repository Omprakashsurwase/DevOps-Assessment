apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helnchart.fullname" . }}-test-connection"
  labels:
    {{- include "helnchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helnchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
